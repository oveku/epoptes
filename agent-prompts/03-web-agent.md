# Web agent task

Improve the React dashboard for Epoptes.

Views required:

1. Overview cards:
   - sessions today
   - total tokens today
   - failed tool calls
   - average session duration

2. Session list:
   - trace id
   - agent
   - model
   - repository
   - duration
   - tokens
   - tools
   - errors

3. Session detail:
   - span timeline
   - token usage
   - tool calls
   - error messages

Keep styling clean, dark and local-dashboard-like. Do not make this look like a SaaS landing page.
